# أسرار — لا تُرفع أبدًا (قاعدة أمان Vibe Coding)
.env

# بايثون
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# بيانات تشغيل محلية (قواعد متعددة الملفات لا تدخل git — درس تلف HNSW الموثق)
data/
chroma_db/
checkpoints/
inbox/*
!inbox/.gitkeep

# قاعدة القرارات ثنائية ويعاد توليدها بكل تشغيلة — لا تُتتبَّع.
# (مخرجات الأرشفة والإشعارات النصية تُلتزم كدليل على تنفيذ فعلي.)
archive/*.sqlite*

# reports/generated/ **يُلتزم** كدليل تقييم (كل الوثائق اصطناعية بلا PII حقيقي).
# لو شُغّلت وثائق حقيقية مستقبلًا: لا تلتزم مخرجاتها (قد تحمل PII — قاعدة R021).

# أدوات
.vscode/
.idea/
