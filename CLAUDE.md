# CLAUDE.md — المشروع الختامي: وكيل دورة حياة الوثيقة المؤسسية

مشروع الدورة الختامي (الفكرة الرابعة المدموجة). ريبو مستقل على GitHub:
`https://github.com/AK7Amin/SDAIA-Advanced-Agentic-AI-Systems-Engineering`

## ⛔ قاعدة سلامة قصوى — تسبق كل شيء

**لا تحذف أي ملف خارج مجلد هذا المشروع**
`C:\Users\abdul\Desktop\Islamic Content Association\SDAIA Advanced Agentic AI Systems Engineering`.
الحذف/التعديل مسموح **داخل** هذا المجلد فقط. أي شيء خارجه (بقية مشاريع
الجمعية، v3، الدورتان السابقتان، الريبو الأم) **يُقرأ فقط، ولا يُكتب ولا يُحذف أبدًا**.

**القراءة**: خذ راحتك في القراءة من أي مكان **إلا البيانات الشخصية والحساسة**
(الهويات، الآيبان، العقود بمبالغها، الرواتب، ملفات الموارد البشرية —
قاعدة R021 في CLAUDE.md الأم). الفرز بالاسم لا بفتح المحتوى.

## ما هو المشروع

وكيل دورة حياة الوثيقة: وثيقة تصل `inbox/` ← **مصنّف** يحدد نوعها ←
**مستخرِج** يخرج البنود (Pydantic) ← **مدقق سياسات** يقارنها بسياسات في
ChromaDB ← **حافة شرطية**: مطابقة تُؤرشف، مخالفة/مشكوكة تُصعَّد لعقدة موافقة
بشرية (breakpoint) ← تُحفظ الحالة (checkpointer) فتُستأنف لاحقًا ← **صائغ**
Jinja2 يخرج الإشعار. طوال المسار: أثر تدقيق + تكلفة وزمن + حواجز أمنية.

يجمع مادة الفكرة 1 (عقود/امتثال) بهيكل الفكرة 3 (LangGraph بحالة وأدوار).
التفصيل: `docs/rubric-predicted.md` و`plan.md`.

## قواعد العمل الملزمة

1. **قواعد Vibe Coding التسع** سارية (النص في `..\..\SDAIA VibeCoding\To Do Project\CLAUDE.md`):
   لا أسرار في الكود (`.env` فقط، مُدرج في `.gitignore`)، TDD صارم
   (RED→GREEN→REFACTOR)، تفكيك ونقاط تحقق، بوابات مراجعة بشرية.
2. **قاعدة الكتابة**: كل مصطلح عربي بأصله الإنجليزي، المختصرات مفكوكة.
3. **git تدريجي**: التزامات صغيرة ذات معنى (لا رفعة واحدة ضخمة) — بند تقييم
   صريح في rubric الدورة السابقة.
4. **مكتبات حقيقية فقط**: langgraph وchromadb وcheckpointer حقيقية — المحاكاة
   لا تكسب البند (قاعدة تقييم منقولة من الدورة السابقة).
5. **أثبت مسارات الفشل**: حقن يُحجب فعلًا، عملية تُقتل وتُستأنف فعلًا.

## البيئة

- venv جاهزة: `C:\Users\abdul\sdaia-agents-venv` (Python 3.12، langgraph 1.x،
  chromadb، fastapi، prometheus-client، pypdf، jinja2 — مثبتة ومختبرة).
  **أُضيف** `langgraph-checkpoint-sqlite` (لم يكن ضمن الحزمة الأصلية).
- **مزلق SqliteSaver مثبت بالتجربة**: يُبنى بـ
  `SqliteSaver(sqlite3.connect(path, check_same_thread=False))` مباشرة —
  **ليس** context manager (لا `with`)، و`from_conn_string` وحده هو الـCM.
- **الاستئناف عبر عملية جديدة يعمل** (مُختبَر بـspike): `Command(resume=...)`
  بنفس thread_id، والتصعيد مفصول: `escalate` (تكتب وترجع) ثم `human_gate`
  (كل جسمها بعد `interrupt()`) — يمنع تكرار أحداث التدقيق عند الاستئناف.
- النموذج: **OpenRouter بنماذج مجانية** (`openai/gpt-oss-20b:free` — مُختبَر
  ويولّد عربية صحيحة). المفتاحان في `.env` من v3 يعملان (اختُبرا).
- طبقة مزود قابلة للتبديل: `LLM_MODEL` في `.env` يغيّر النموذج بلا لمس كود.
- الوثائق والسياسات **عربية** + وثائق مُصطنعة نكتبها نحن (لا ملفات جمعية حقيقية).

## سير العمل المتبع (من مقطعَي «فقه البرمجة»)

خطة ← (تقييم 3 وكلاء ← إصلاح الخطة) ← حلقة تنفيذ↔تحقق حتى الاكتمال ←
مراجعة نهائية بالrubric. حد أقصى للانتقالات يمنع الحلقة اللانهائية.
النصوص الكاملة:
`AI Managing Assistant v3\docs\research\transcripts\fikhul-barmaja\{H03gPAZOrBg,vhbGUwoCXB8}.txt`
