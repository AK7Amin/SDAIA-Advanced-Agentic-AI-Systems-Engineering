# خطة البناء — وكيل دورة حياة الوثيقة المؤسسية (v2)

> **v2 بعد نقد الوكلاء الثلاثة** — التعديلات الكاملة في `docs/critique-round-1.md`.
> تُقرأ مع `docs/rubric-predicted.md` (كل مرحلة موسومة ببند الrubric الذي تثبته).
> منهجية: TDD — اختبارات كل قطعة قبل كودها. حلقة تنفيذ↔تحقق لكل قطعة بحد
> أقصى 4 انتقالات، ثم توثيق العجز والانتقال (لا حلقات لانهائية).

## تعديلات v2 المعتمدة (مدموجة من النقد — انظر critique-round-1.md للتفصيل)

- **المخطط**: التصعيد عقدتان `escalate`→`human_gate` (interrupt بعد كتابة
  الحالة)، والاستئناف `Command(resume=...)`. plan_route **يغيّر التحكم فعلًا**
  (قرار المخطِّط LLM يوجّه الحافة — مُثبت باختبار `test_planner_decision_drives_control_flow`).
  المخطط يُجمَّع دومًا بـcheckpointer. **[مُثبت بـspike حقيقي عبر عمليتين]**.
- **الأمن**: `raw_text` لا يدخل الحالة المُنقَّطة (فقط `masked_text`) — لا PII
  في الcheckpoint. تعقيم الوثيقة بالتغليف لا الحذف. مُنقٍّح مفاتيح مركزي.
  مضمِّن ChromaDB محلي مثبت. سطح الاختراق: حقن مباشر/غير مباشر + استخراج
  system prompt عبر notify + تسميم السياسات + اجتياز مسار + استنزاف حجم.
- **الأدلة**: علم `--no-guardrails` لدليل «قبل/بعد». تكلفة/توكنز/كمون **لكل
  وثيقة** تُحفظ في `reports/metrics-snapshot.json`. traces كملفات
  `reports/traces/<doc_id>.json`. لوحة HTML تقرأ الملف المحفوظ.
- **التحوّط**: `Dockerfile` + `requirements.txt` مثبت + endpoint `POST /process`.
  مخطط Mermaid + متطلبات GitHub الست في README.
- **SqliteSaver**: حزمة `langgraph-checkpoint-sqlite` (ثُبتت)، تُبنى مباشرة
  `SqliteSaver(sqlite3.connect(path, check_same_thread=False))` بلا `with`.

## 0. القرارات المعمارية

| قرار | الاختيار | السبب |
|---|---|---|
| الإطار | LangGraph 1.x (StateGraph) | إطار الدورة الرسمي + بند rubric 1 |
| النموذج | OpenRouter `openai/gpt-oss-20b:free` عبر `langchain-openai` (base_url مخصص) | قرار المالك؛ مجاني؛ مُختبَر بالعربية |
| طبقة LLM | `src/llm.py` واحدة: مصنع نموذج + غلاف عدّاد توكنز/تكلفة/كمون | تبديل نموذج بلا لمس كود + بند 5 |
| الحالة | `TypedDict` + reducers، checkpointer = `SqliteSaver` | بند 3؛ Postgres ترقية اختيارية |
| الرسائل البينية | Pydantic models في `src/schemas.py` — كل وكيل يرجع نوعًا محددًا | بند 2 (typed outputs) |
| الذاكرة المتجهية | ChromaDB (client محلي) لفهرسة السياسات policies | بند 3 + خبرة v3 |
| الوثائق | عربية مُصطنعة: عقود/فواتير/خطابات (نكتبها) + سياسات عربية | قرار المالك |
| الاختبارات | pytest، الوكلاء بـLLM موهوم mock (حتمي) — التشغيل الحي منفصل | TDD بلا حرق رصيد |
| human-in-the-loop | `interrupt()` حقيقي من LangGraph عند عقدة الموافقة | بند 1+3، والاستئناف عبر عملية جديدة |

## 1. المخطط graph (قلب المشروع — بنود 1+2)

```
inbox → [ingest] → [classify] → (conditional: نوع معروف؟)
    نعم → [plan_route] → [extract] ...   ← عقدة تخطيط: النموذج يقرر مخطط
                                            الاستخراج وحزمة السياسات حسب النوع
                                            (Plan-and-Execute حقيقي لا شكلي)
    لا → [quarantine] → END (سجل + سبب)
    نعم → [extract] → (conditional: اكتمال الحقول؟)
        ناقص + محاولات<2 → [extract] مرة أخرى بتلميح (حلقة!)
        ناقص + محاولات=2 → [escalate]
        كامل → [policy_check] → (conditional: الحكم؟)
            مطابق → [archive] → [notify] → END
            مخالف/مشكوك → [escalate] → interrupt() ← موافقة بشرية
                موافقة → [archive] → [notify] → END
                رفض → [reject] → [notify] → END
```

- الحلقة المطلوبة لبند 1: إعادة الاستخراج بتلميح عند نقص الحقول (بعداد).
- كل عقدة تكتب في `state["audit_trail"]` (قائمة أحداث ممهورة بالوقت) —
  reducer يجمع ولا يستبدل، وكل حدث يحمل **hash الحدث السابق** (سلسلة تجزئة
  hash-chained تجعل الأثر غير قابل للتعديل بأثر رجعي — نص الفكرة 1 حرفيًا).
- **حاجز ميزانية**: حد أقصى لنداءات النموذج لكل وثيقة (يفشل بصوت عالٍ عند
  تجاوزه) — الجواب العملي على «انفجار التكلفة» من شرائح اليوم 5.
- **PII يُقنَّع قبل أي نداء نموذج** (`masked_text` في الحالة) — إسقاط مباشر
  لقاعدة R021 وحاجز «حماية البيانات» من شرائح اليوم 4.
- الوكلاء (بند 2): classifier / extractor / policy_checker / notifier —
  كل واحد وحدة مستقلة في `src/agents/` بعقد Pydantic دخولًا وخروجًا،
  والمنسق هو المخطط نفسه (LangGraph supervisor صريح).

## 2. مراحل البناء (كل مرحلة: اختبارات ← كود ← تشغيل ← commit)

| م | القطعة | يثبت بند | الاختبارات الرئيسة |
|---|---|---|---|
| M1 | `schemas.py`: أنواع الوثائق، حقول الاستخراج، حكم التدقيق، حدث تدقيق | 2 | صحة/رفض حقول، أنواع الحكم الثلاثة |
| M2 | `llm.py`: مصنع + عداد (tokens/cost/latency) + fallback مفتاح ثانٍ عند 402/403 | 5 | العداد يجمع؛ fallback يتحول عند 403 (موهوم) |
| M3 | `agents/classifier.py` + `agents/extractor.py` (موهومان في الاختبار) | 2 | مخرجات typed؛ فشل parsing يرفع استثناء محدد |
| M4 | `policies/` عربية + `agents/policy_checker.py` مع ChromaDB حقيقي | 3 | فهرسة واسترجاع سياسة صحيحة لبند معطى |
| M5 | `graph/build.py`: العقد والحواف الشرطية وحلقة الاستخراج | 1 | كل مسار من الرسم أعلاه يمر (بوكلاء موهومين): مطابق، مخالف، نوع مجهول، نقص→حلقة→نجاح، نقص→حلقة→تصعيد |
| M6 | checkpointing: SqliteSaver + `interrupt()` للموافقة | 3 | يتوقف عند escalate؛ **عملية بايثون ثانية** تستأنف بنفس thread_id وتكمل |
| M7 | `guardrails/`: فحص مدخلات (أنماط حقن عربية/إنجليزية) + مخرجات (PII masking) + صلاحيات أدوات | 4 | حقن مباشر يُحجب؛ **وثيقة تحمل حقنًا غير مباشر** تُعلَّم وتُعقَّم قبل وصولها للنموذج؛ رقم هوية يُقنَّع في الإشعار |
| M8 | `observability/`: prometheus counters/histograms + endpoint `/metrics` + صفحة لوحة HTML بسيطة | 5 | العدادات تزيد مع كل عقدة؛ endpoint يستجيب |
| M9 | `main.py` CLI: `run` (معالجة inbox)، `resume <thread_id>` (بعد موافقة)، `attack` (سيناريو الاختراق) | 6 | دخان end-to-end بوكلاء موهومين |
| M10 | تشغيل حي: 6 وثائق مُصطنعة عبر OpenRouter الحقيقي + التقاط السجلات في `reports/live-run.md` | قاعدة «شغّل واحتفظ» | — |
| M11 | `reports/pentest-report.md`: الهجمات قبل/بعد + `reports/metrics-snapshot` | 4+5 | — |
| M12 | README (إنجليزي، متطلبات GitHub الست) + `run.ps1` + docker-compose اختياري + `demo.md` سيناريو العرض | 6 | — |

## 3. سيناريو الدمو (يُبنى له من البداية)

1. `python main.py run` — 3 وثائق: عقد مطابق (يُؤرشف)، فاتورة تتجاوز حد
   السياسة (تتجمد بانتظار موافقة)، ملف نصي عشوائي (حجر).
2. إغلاق العملية كليًا ← `python main.py resume <id> --approve` من عملية
   جديدة — يكمل من نقطة التجمد (بند 3 أمام اللجنة).
3. `python main.py attack` — حقن مباشر + وثيقة مفخخة: تُحجب وتُسجل (بند 4).
4. فتح لوحة القياسات: توكنز/تكلفة/كمون لكل وثيقة (بند 5).

## 4. حدود الليلة

- MinIO وPostgres وGrafana: **خارج النطاق الليلة** (ترقيات موثقة في README) —
  SqliteSaver ومجلد inbox وصفحة HTML يفون بالبنود.
- لا نصوص PDF: الوثائق المُصطنعة `.md/.txt` (استخراج PDF معروف عندنا من v3
  ويُضاف لاحقًا إن طلبه rubric المهندس الحقيقي).
