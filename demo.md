# سيناريو العرض + قائمة تحقق قبل الدمو

## قائمة تحقق قبل العرض (الليلة السابقة)

- [ ] `pip install -r requirements.txt` في بيئة نظيفة ينجح.
- [ ] `python -c "from src.policy_store import PolicyStore; PolicyStore().index_policy_file('policies/procurement_policy.md')"`
      مرة واحدة لتسخين تنزيل مضمِّن ChromaDB (~80MB) — لئلا يعلق على شبكة القاعة.
- [ ] افحص حصة المزوّد. إن نفدت، بدّله من `.env` وحده (`LLM_BASE_URL` و
      `LLM_MODEL` و`LLM_API_KEY`) — لا كود يتغير. هذا نفسه دليل قابلية التبديل.
- [ ] `python main.py run` مرة واحدة لتوليد أدلة حديثة، ثم `verify-traces`
      للتأكد أن الآثار سليمة. **لا تُشغّل التقاطين متوازيين**.
- [ ] افتح `reports/live-run.md` احتياطيًا: إن سقط المزوّد أثناء العرض، اعرض
      الأدلة المحفوظة بدل الارتجال.

## السيناريو (4 دقائق)

1. **المعالجة الآلية** — `python main.py run`
   سبع وثائق: عقد مطابق → أُرشف، فاتورة متجاوزة → توقفت للموافقة، وثيقة مفخوخة
   → عُلِّم حقنها وصُعّدت، ضجيج → حُجر، وعقدا PDF عربيان يُقرآن فعلًا.
   أشر إلى العمودين: `أدوات=` (النموذج استدعى أدوات حقيقية) و`مصدر=` (من قرر).

2. **الاستئناف عبر عملية** — انسخ معرّف الخيط الذي طبعه `run`:
   `python main.py resume <thread_id> approve`
   من عملية جديدة كليًا؛ الفاتورة تكمل إلى الأرشفة من نقطة التوقف. وجرّب
   `reject` على وثيقة أخرى ليُرى الفرعان.

3. **الاختراق قبل/بعد** —
   `python main.py attack --no-guardrails` (يمر) ثم `python main.py attack` (يُحجب).

4. **سلامة الأثر** — `python main.py verify-traces`: فحص يعيد حساب سلسلة
   التجزئة من الأحداث ولا يثق بما كُتب في الملف.

5. **اللوحة** — افتح `reports/generated/dashboard.html`: توكنز/كمون/تكلفة لكل وثيقة.

## جملة الافتتاح (٣٠ ثانية)

«بنينا وكيل دورة حياة الوثيقة: يصنّف ويستخرج ويدقّق الامتثال، ويصعّد المخالف
لموافقة بشرية يتوقف عندها ويستأنف عبر الأيام. القرار بيد النموذج عبر مخطط حالة
LangGraph بحواف شرطية وحلقة، مع حواجز أمنية وأثر تدقيق غير قابل للعبث ومراقبة
تكلفة — كله على بيئة محاكاة ووثائق اصطناعية.»

## سؤالان متوقعان وجوابهما

- **«كيف أعرف أن النموذج استدعى الأداة فعلًا ولم تُبرمج النتيجة؟»**
  افتح أي أثر: كل استدعاء يحمل وسائطه المتحقَّق منها ومخرجها الفعلي ووسم
  `[مصدر=model]`. وحين يفرض النظام الاستدعاء يُوسم `policy_enforced` صراحةً.

- **«ماذا لو عبث أحد بالسجل؟»** كل حدث يحمل بصمة سابقه؛ `verify-traces` يكشف
  أي تعديل بأثر رجعي — وقد كشف فعلًا عيبًا حقيقيًا في التطوير.
